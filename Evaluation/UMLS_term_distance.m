X = dlmread('UMLS_term_mat');
diseases = X(1:753,201:300);
symptoms = X(754:1234,201:300);
treatments = X(1235:1751,201:300);
[nd,d] = size(diseases);
[ns,d] = size(symptoms);
[nt,d] = size(treatments);
d = sum(diseases)/nd;
distance_d = sum(sqrt(sum((diseases-ones(nd,1)*d).^2, 2)))/nd
s = sum(symptoms)/ns;
distance_s = sum(sqrt(sum((symptoms-ones(ns,1)*s).^2, 2)))/ns
t = sum(treatments)/nt;
distance_t = sum(sqrt(sum((treatments-ones(nt,1)*t).^2, 2)))/nt
distance_ds = sqrt(sum((d-s).^2, 2))
distance_dt = sqrt(sum((d-t).^2, 2))
distance_st = sqrt(sum((s-t).^2, 2))
db_ds = (distance_d+distance_s)/distance_ds;
db_dt = (distance_d+distance_t)/distance_dt;
db_st = (distance_s+distance_t)/distance_st;
db = (max(db_ds,db_dt)+max(db_ds,db_st)+max(db_dt,db_st))/3
m = max([distance_d,distance_s,distance_t]);
d = min([distance_ds/m,distance_dt/m,distance_st/m])